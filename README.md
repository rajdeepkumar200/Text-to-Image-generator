# Text-to-Image-generator
Key Features - Uses a powerful pre-trained Stable Diffusion model,  Accepts natural language prompts for image generation,  Simple and minimal — runs with just a few lines of code, Uses diffusers, transformers, and torch for efficient performance,  Integrated with Hugging Face login to access gated models.
